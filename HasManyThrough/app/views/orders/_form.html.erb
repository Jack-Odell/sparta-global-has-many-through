<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :starter %>
    <%= form.text_field :starter %>
  </div>

  <div class="field">
    <%= form.label :main %>
    <%= form.text_field :main %>
  </div>

  <div class="field">
    <%= form.label :beverage %>
    <%= form.text_field :beverage %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= collection_select(:order, :user_id, User.all, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :restaurant_id %>
    <%= collection_select(:order, :restaurant_id, Restaurant.all, :id, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
